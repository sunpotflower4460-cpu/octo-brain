# 本番ビルド用 (npm run build)。Capacitor はこのビルド出力(dist)を同梱する。
# ↓ 本番 Cloudflare Workers の URL に差し替える(例: https://octo-brain.<account>.workers.dev)。
# 未設定/SET_ME のままだと本番アプリが API に到達できないので、申請前に必ず設定すること。
VITE_API_BASE=SET_ME_PRODUCTION_WORKERS_URL
